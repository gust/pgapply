{
  "name": "pgdeploy",
  "version": "0.1.0",
  "description": "automated Postgres deployment from Git",
  "bin": "output/main.js",
  "scripts": {
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1",
    "test:server": "sudo docker run --rm -p5432:5432 -e POSTGRES_PASSWORD=password postgres:9.2"
  },
  "author": "",
  "license": "UNLICENSED",
  "private": true,
  "devDependencies": {
    "@types/es6-promise": "0.0.32",
    "@types/node": "^4.0.30",
    "typescript": "^2.0.3",
    "typings": "^1.4.0"
  },
  "dependencies": {
    "pg-promise": "^5.3.4"
  }
}
